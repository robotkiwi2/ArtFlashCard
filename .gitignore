# OS / 동기화 부산물
Thumbs.db
desktop.ini
.DS_Store

# 임시 테스트 파일
__test.html
*.tmp

# 원본 업로드 파일 (cards.csv로 병합 반영됨)
data/cards-v2.csv

# 작업 중 스냅숏 (복구 지점은 git 히스토리로 충분하다)
data/cards.backup-*.csv

# 검증용 임시 페이지
_*.html

# Firebase 서비스 계정 키 (절대 커밋 금지)
firebase-admin-key.json
*-firebase-adminsdk-*.json

# 카드 원본 데이터 — 저장소에 올리지 않는다 (Firestore 로 배포, 로컬 사본은 OneDrive 가 보관)
data/*.csv

# 파이썬 캐시
__pycache__/
