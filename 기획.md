# 임용미술 플래시카드 기획

## 개요
- 중등미술 임용고시 대비 웹 기반 플래시카드 학습 도구
- **GitHub Pages 정적 페이지**로 배포 (서버/DB/로그인 없음)
- 데이터는 CSV 파일, 학습 기록은 브라우저 **localStorage**에 저장

## 데이터 스키마 (`data/cards.csv`)

| 컬럼 | 설명 | 예시 |
|---|---|---|
| id | 고유 번호. **절대 재부여하지 않는다** (학습 기록의 키) | 1 |
| 표제어 | 카드의 정체 | 갈필 |
| 표제어변형 | 한자·외국어 등 원어 표기 (없으면 빈 값) | 渴筆 / 乾筆 |
| 축1~축4 | **이 카드가 물릴 수 있는 출제 각도.** `라벨: 내용` 형식 | `기법: 붓의 수분을 제한하여...` |
| 캡션 | **작품 카드 전용.** 작가·제목·연도 | 정선, 〈인왕제색도〉, 1751 |
| 이미지링크 | 도판 URL (없으면 빈 값) | https://... |
| 중요도 | 1~4 등급 (1이 최상) | 1 |
| 과목 | 일반교육학 / 미술교육학 / 서양미술사 / 동양미술사 / 한국미술사 / 표현기법 (6과목) | 한국미술사 |
| 유형 | 기법 / 인물 / 작품 / 사조 / 원리 / 개념 / 교육과정 | 기법 |
| 시대 | 해당 없는 카드는 빈 값 | 조선후기 |
| 태그 | 세미콜론(;) 구분 다중값 | 문인화;수묵 |
| 출처 | 기출 출처 `연도+과목+문항`, 세미콜론 구분 | 2024A9;2022A3 |

> `짝` 컬럼은 폐지하고 아래의 **관계 테이블**(`data/links.csv`)로 옮겼다.

## 관계 테이블 (`data/links.csv`)

관계를 카드 안에 심지 않고 **카드 &mdash; 관계 &mdash; 카드** 형태의 별도 테이블로 둔다.
관계 종류가 늘어나도 카드 스키마는 그대로이고, 카드 편집과 관계 편집이 서로 간섭하지 않는다.

| 컬럼 | 설명 | 예 |
|---|---|---|
| 출발 | 표제어 | 부벽준 |
| 관계 | 아래 표의 관계 종류 | 대비 |
| 도착 | 표제어 | 피마준 |
| 메모 | 왜 이렇게 묶이는지 한 줄 (선택) | 남종 부드러운 선 ↔ 북종 각진 면 |

### 한 행이 양방향으로 동작한다

관계마다 역방향에서 붙일 이름을 사전(`REL_REVERSE`)으로 정해 두었다.
`준법 —상위→ 부벽준` 한 줄을 쓰면, 부벽준 카드에서는 **하위: 준법**,
준법 카드에서는 **상위: 부벽준** 으로 자동으로 뒤집혀 보인다.

| 관계 | 역방향 | 성격 |
|---|---|---|
| 관련 | 관련 | 대칭 (구관계에서 옮겨 온 기본값) |
| 대비 | 대비 | 대칭 |
| 상위 | 하위 | 비대칭 |
| 계보 | 선행 | 비대칭 |
| 영향 | 영향받음 | 비대칭 |
| 작품 | 작가 | 비대칭 |
| 기법 | 사용례 | 비대칭 |
| 주장 | 주창자 | 비대칭 |

새 관계 종류는 `REL_REVERSE`에 한 줄 추가하면 끝이다.

### 태그와 역할을 나눈다 &mdash; 중요

- **태그** → 같은 갈래 안의 **형제**를 자동으로 만든다. 데이터 추가 비용이 0이다.
  (`siblings()`가 같은 태그를 공유하는 카드를 찾는다. 8장을 넘는 넓은 태그는 형제로 보지 않고 제외한다.)
- **관계 테이블** → 태그로는 잡히지 않는 것만 손으로 적는다.
  갈래를 가로지르는 대비, 시간 흐름(계보), 위계(상위/하위), 종류가 다른 것끼리(인물↔작품↔기법).

> 준법 5형제를 서로 잇는 데 10행을 쓰면 낭비다. 그건 태그가 이미 해 준다.
> 관계 테이블에는 `준법 —상위→ 각 준법` 6행만 두면 위계까지 완성된다.

- 카드 뒷면에는 이미 관계로 이어진 카드를 태그 형제 목록에서 빼서 중복을 없앤다.
- 같은 종류의 관계는 한 줄로 묶어 보여 준다 (상위 6줄 → 상위 1줄).
- 같은 쌍에 구체적 관계(대비 등)가 있으면 포괄적인 `관련` 행은 두지 않는다.

### 참조 무결성

관계 테이블은 표제어로 참조하므로, 카드의 표제어를 바꾸면 조용히 끊어진다.
로드할 때 카드에 없는 표제어는 버리고 **콘솔에 경고**를 남긴다(`[links] 카드에 없는 표제어 참조:`).
표제어를 고칠 때는 `links.csv`도 함께 확인할 것.

### UI 원칙

회전 중에 링크가 잔뜩 뜨면 진도가 나가지 않는다. 뒷면 하단에 **`▸ 관련 항목 N`** 버튼으로
접어 두고, 눌렀을 때만 펼친다.

### 축(출제 각도)을 쓰는 이유

기출 분석 결과 재출제는 **정답 문자열이 아니라 인물·개념 단위**로 일어나고,
묻는 각도는 매번 바뀐다. (예: 에플랜드는 2019·2022·2023·2024에 출제되었으나
매번 다른 것을 물었다.) 따라서 카드 하나에 여러 축을 매달아 둔다.

유형별 권장 라벨:

| 유형 | 축1 | 축2 | 축3 | 축4 |
|---|---|---|---|---|
| 인물 | 대표 주장 | 핵심 용어 | 저서·대표작 | 대비되는 인물 |
| 작품 | 기법 | 사조·양식 | 조형 특징 | 의미·상징 |
| 기법 | 정의 | 재료·도구 | 효과 | 혼동하기 쉬운 것 |
| 사조 | 시기·배경 | 조형 특징 | 대표 작가 | 선행·후행 사조 |
| 개념 | 정의 | 출제 포인트 | — | — |
| 교육과정 | 원문(빈칸 처리) | 빈칸 답 | — | — |

- **축1만 필수.** 축2~4는 기출에서 실제로 물은 적이 있을 때만 채운다.
  빈 축은 결함이 아니라 "아직 출제된 적 없는 각도"라는 정보다.
- 라벨은 12자 이내여야 배지로 렌더된다. 콜론이 없으면 본문으로만 표시된다.
- 태그에는 유형·시대 외의 자유 키워드만 넣는다 (유형/시대는 별도 컬럼).
- 표제어변형은 표제어가 노출되는 모든 위치에서 아래에 작게 함께 표시된다.

> **중요도는 과도기 상태다.** 현재 값(1등급 223장 / 2등급 243장)은 사실상 반반이라
> 변별력이 없다. `출처`가 충분히 채워지면 그 개수에서 산출한 **빈출도**로 값을 교체할
> 예정이다. 컬럼명을 유지하는 이유는 필터·통계 코드를 건드리지 않기 위해서다.

### 필터 동작
- **과목을 먼저 선택**하면 유형·시대·태그·중요도 칩이 해당 과목 카드에서만 재생성된다
  (서양미술사 + 고려시대 같은 무의미한 조합 방지).
- 과목 변경 시 새 범위에 없는 하위 선택은 자동 해제된다.
- 태그는 [모두 선택] / [해제] 버튼 제공, 영역은 스크롤 처리.
- 시대·태그가 **비어 있는 카드는 해당 축의 선택과 무관하게 항상 통과**한다.
  값이 있는 카드만 필터로 걸러진다 (`app.js`의 `if (c.시대) { ... }` 패턴).
  전체 504장 중 시대가 빈 카드가 256장(51%)이므로, 시대를 좁혀도 이들은 남는다.
- 미학은 별도 과목이 아니라 미술교육학 안의 `미학` 태그로 구분한다.
- 버튼·칩 클릭 시 Web Audio API로 생성한 짧은 클릭음 재생 (음원 파일 불필요).

### 글자 크기
- 카드 본문 배율을 CSS 변수 `--fs`로 제어하며 4단계 제공
  (작게 1.0 / **보통 1.2 = 기본** / 크게 1.45 / 아주 크게 1.75)
- 선택값은 localStorage(`flashcard-fontsize-v1`)에 저장되어 다음 방문에도 유지
- 모바일 360px 폭에서 네 단계 모두 텍스트 넘침 없음을 확인

## 학습 흐름

1. **범위 설정**: 과목 / 유형 / 시대 / 태그 / 중요도(1~4 범위)로 카드 풀 필터링
2. **분량 선택**: 진행할 카드 수 선택 (필터 결과 내에서 랜덤 추출)
3. **모드 선택**
   - **표제어 제시**: 앞면 표제어 → [답] 누르면 캡션 + 모든 축 (+이미지)
   - **설명 제시**: 앞면 **축1만** → [답] 누르면 표제어 + 캡션 + 나머지 축 (+이미지)
   - **이미지 제시**: 이미지링크 있는 카드만 대상.
     앞면 **이미지 + 캡션** → [답] 누르면 표제어 + 모든 축

   > 이미지 모드에서 캡션(작가·제목·연도)을 앞면에 함께 주는 것은 실제 시험 구조를
   > 그대로 옮긴 것이다. 기출 도판 28문항 중 절반가량이 캡션을 함께 제시하며,
   > 물어보는 것은 작가·제목이 아니라 기법·사조·조형 특징이다.

   - 앞면에는 **시대를 표시하지 않는다** (답을 흘리게 된다). 시대는 뒷면에만 나온다.
   - 뒷면 하단에 `짝`(함께 출제되는 상대)과 `출처`(기출 N회)가 함께 표시된다.
4. **자가 채점**: 답 공개 후 [맞음] / [틀림] 버튼 → 기록 저장 후 다음 카드
5. **세션 종료**: 이번 세션 정답률 요약 표시

## 출제 가중치 (반복 학습 최적화)

맞힌 카드가 계속 되풀이되지 않도록 라이트너 상자 방식의 가중 추출을 사용한다.

- 카드마다 `box`(연속 정답 횟수, 0~5)를 기록. 맞히면 +1, 틀리면 0으로 초기화
- 가중치 = `8 / 2^box` → 미학습 8 · 1회 정답 4 · 3연속 1 · 5연속 0.25
- 오답 상태(`wrong`)면 가중치 2배
- 마지막 학습 이후 경과일에 따라 최대 3배까지 회복 (30일에 3배) → 망각 반영
- 세션 카드는 이 가중치로 비복원 추출

> 검증: 맞힌60·미학습30·오답10 풀에서 300세션 추출 시
> 맞힌 카드 60%→15%, 오답 10%→33%로 조정됨

## 오답 노트

- [학습 시작] 옆에 [오답 노트 (N)] 버튼 배치. 오답이 없으면 비활성
- 누르면 **필터 범위를 무시하고** `wrong=true`인 카드만 출제
- 정답 처리하면 `wrong=false`가 되어 오답 노트에서 제외됨
  (오답 노트 세션뿐 아니라 일반 세션에서 맞혀도 동일하게 해제)
- 이미지 제시 모드일 때만 이미지 보유 오답으로 한정

## 성적 관리 (localStorage)

- 카드 단위로 저장: `{ 시도횟수, 정답횟수, box, wrong, 마지막학습일 }`
- 통계 화면에서 과목/유형/태그별로 그룹핑하여 수행문제수·정답률 집계
- 태그별 정답률은 "약점 신호" 용도 (한 카드가 여러 태그에 걸침)
- 기록 초기화 버튼 제공
- (추후) JSON export/import로 기기 간 이동 지원

## 배포

- 저장소: https://github.com/robotkiwi2/ArtFlashCard
- GitHub Pages를 `main` 브랜치 `/ (root)` 로 지정하여 배포
  (Pages가 루트를 서빙하므로 앱 파일은 저장소 최상단에 둔다)
- 주의: Pages 무료 플랜은 public 저장소 → 카드 데이터가 공개됨.
  비공개가 필요해지면 private 저장소 + 다른 호스팅(Cloudflare Pages 등) 검토

## 로컬 실행

브라우저가 file:// 에서 CSV fetch를 막으므로 간이 서버로 실행:

```
cd flashcard
python -m http.server 8123
# 브라우저에서 http://localhost:8123 접속
```
