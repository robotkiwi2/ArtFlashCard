# 임용미술 플래시카드 기획

## 개요
- 중등미술 임용고시 대비 웹 기반 플래시카드 학습 도구
- **GitHub Pages 정적 페이지**로 배포 (서버/DB/로그인 없음)
- 데이터는 CSV 파일, 학습 기록은 브라우저 **localStorage**에 저장

## 데이터 스키마 (`data/cards.csv`)

| 컬럼 | 설명 | 예시 |
|---|---|---|
| id | 고유 번호 | 1 |
| 표제어 | 카드 앞면 용어 | 갈필 |
| 표제어변형 | 한자·외국어 등 원어 표기 (없으면 빈 값) | 渴筆 / 乾筆 |
| 설명 | 카드 뒷면 설명 | 붓의 수분을 제한하여 거칠게 긋는 필법... |
| 이미지링크 | 도판 URL (없으면 빈 값) | https://... |
| 중요도 | 1~4 등급 (1이 최상) | 1 |
| 과목 | 일반교육학 / 미술교육학 / 서양미술사 / 동양미술사 / 한국미술사 / 표현기법 (6과목) | 한국미술사 |
| 유형 | 기법 / 인물 / 작품 / 사조 / 원리 / 개념 등 | 기법 |
| 시대 | 해당 없는 카드는 빈 값 | 조선후기 |
| 태그 | 세미콜론(;) 구분 다중값 | 문인화;수묵 |

- 태그에는 유형·시대 외의 자유 키워드만 넣는다 (유형/시대는 별도 컬럼).
- 표제어변형은 표제어가 노출되는 모든 위치에서 아래에 작게 함께 표시된다.
- 추후 "출처"(기출연도 등) 컬럼 추가 고려.

### 필터 동작
- **과목을 먼저 선택**하면 유형·시대·태그·중요도 칩이 해당 과목 카드에서만 재생성된다
  (서양미술사 + 고려시대 같은 무의미한 조합 방지).
- 과목 변경 시 새 범위에 없는 하위 선택은 자동 해제된다.
- 태그는 [모두 선택] / [해제] 버튼 제공, 영역은 스크롤 처리.
- 시대·태그가 비어 있는 카드는 해당 축을 **전부 선택해 둔 동안에만** 통과한다.
  일부만 골라 범위를 좁히면 분류 불가능한 카드로 보고 제외 (예: 태그를 '미학'만
  선택하면 태그 없는 카드는 빠짐).
- 미학은 별도 과목이 아니라 미술교육학 안의 `미학` 태그로 구분한다.
- 버튼·칩 클릭 시 Web Audio API로 생성한 짧은 클릭음 재생 (음원 파일 불필요).

### 글자 크기
- 카드 본문 배율을 CSS 변수 `--fs`로 제어하며 4단계 제공
  (작게 1.0 / **보통 1.2 = 기본** / 크게 1.45 / 아주 크게 1.75)
- 선택값은 localStorage(`flashcard-fontsize-v1`)에 저장되어 다음 방문에도 유지
- 모바일 360px 폭에서 네 단계 모두 텍스트 넘침 없음을 확인

## 학습 흐름

1. **범위 설정**: 과목 / 유형 / 시대 / 태그 / 중요도(1~4 범위)로 카드 풀 필터링
2. **분량 선택**: 진행할 카드 수 선택 (필터 결과 내에서 랜덤 추출)
3. **모드 선택**
   - **표제어 제시**: 앞면 표제어 → [답] 누르면 설명 (+이미지 있으면 함께)
   - **설명 제시**: 앞면 설명 → [답] 누르면 표제어 (+이미지 있으면 함께)
   - **이미지 제시**: 이미지링크 있는 카드만 대상. 앞면 이미지 → [답] 누르면 표제어+설명
4. **자가 채점**: 답 공개 후 [맞음] / [틀림] 버튼 → 기록 저장 후 다음 카드
5. **세션 종료**: 이번 세션 정답률 요약 표시

## 출제 가중치 (반복 학습 최적화)

맞힌 카드가 계속 되풀이되지 않도록 라이트너 상자 방식의 가중 추출을 사용한다.

- 카드마다 `box`(연속 정답 횟수, 0~5)를 기록. 맞히면 +1, 틀리면 0으로 초기화
- 가중치 = `8 / 2^box` → 미학습 8 · 1회 정답 4 · 3연속 1 · 5연속 0.25
- 오답 상태(`wrong`)면 가중치 2배
- 마지막 학습 이후 경과일에 따라 최대 3배까지 회복 (30일에 3배) → 망각 반영
- 세션 카드는 이 가중치로 비복원 추출

> 검증: 맞힌60·미학습30·오답10 풀에서 300세션 추출 시
> 맞힌 카드 60%→15%, 오답 10%→33%로 조정됨

## 오답 노트

- [학습 시작] 옆에 [오답 노트 (N)] 버튼 배치. 오답이 없으면 비활성
- 누르면 **필터 범위를 무시하고** `wrong=true`인 카드만 출제
- 정답 처리하면 `wrong=false`가 되어 오답 노트에서 제외됨
  (오답 노트 세션뿐 아니라 일반 세션에서 맞혀도 동일하게 해제)
- 이미지 제시 모드일 때만 이미지 보유 오답으로 한정

## 성적 관리 (localStorage)

- 카드 단위로 저장: `{ 시도횟수, 정답횟수, box, wrong, 마지막학습일 }`
- 통계 화면에서 과목/유형/태그별로 그룹핑하여 수행문제수·정답률 집계
- 태그별 정답률은 "약점 신호" 용도 (한 카드가 여러 태그에 걸침)
- 기록 초기화 버튼 제공
- (추후) JSON export/import로 기기 간 이동 지원

## 배포

- 저장소: https://github.com/robotkiwi2/ArtFlashCard
- GitHub Pages를 `main` 브랜치 `/ (root)` 로 지정하여 배포
  (Pages가 루트를 서빙하므로 앱 파일은 저장소 최상단에 둔다)
- 주의: Pages 무료 플랜은 public 저장소 → 카드 데이터가 공개됨.
  비공개가 필요해지면 private 저장소 + 다른 호스팅(Cloudflare Pages 등) 검토

## 로컬 실행

브라우저가 file:// 에서 CSV fetch를 막으므로 간이 서버로 실행:

```
cd flashcard
python -m http.server 8123
# 브라우저에서 http://localhost:8123 접속
```
